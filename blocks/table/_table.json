{
  "definitions": [
    {
      "title": "Table",
      "id": "table",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Table",
              "model": "table"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "table",
      "fields": [
        {
          "component": "richtext",
          "valueType": "string",
          "name": "table",
          "label": "Table",
          "description": "The table itself. The first row becomes the header row unless the no-header variant is used."
        },
        {
          "component": "text",
          "valueType": "string",
          "name": "caption",
          "label": "Caption",
          "description": "Optional. Describes the table and names its scrollable region for screen readers."
        }
      ]
    }
  ],
  "filters": []
}
