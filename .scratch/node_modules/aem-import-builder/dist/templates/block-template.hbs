/**
* Create an array of elements for the {{name}} block.
*
* @param {HTMLElement} element The element query from.
* @param {Object} options The options for the block.
* @param {Document} options.document The owner document.
*/
export default function parse(element, { document }) {
  const cells = {{{cells}}};
  return cells;
}
