export type EndpointConfig = {
    spacecatUrl: string;
    githubUrl: string;
};
export type EndpointEnvironment = keyof EndpointDictionary;
type EndpointDictionary = Record<'prod' | 'stage', EndpointConfig>;
export declare const endpointMap: EndpointDictionary;
export {};
