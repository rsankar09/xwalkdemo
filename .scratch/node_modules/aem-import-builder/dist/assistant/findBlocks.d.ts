import { BlockRule } from 'aem-import-rules';
declare function findBlockSelectors(content: string, screenshot: string, prompt: string): Promise<Partial<BlockRule>[]>;
export default findBlockSelectors;
